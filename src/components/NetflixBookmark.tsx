import React from 'react'

interface IProps {
    logout: () => void
    authUser: any
    }

const NetflixBookmark = ({logout}: IProps) => {
    return <div>NetflixBookmark</div>
}
export default NetflixBookmark
